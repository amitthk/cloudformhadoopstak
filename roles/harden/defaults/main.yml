---
RANDOM_PASSWD: |
  "{{ ansible_date_time.iso8601_basic | password_hash('sha512') | password_hash('sha512') }}"
service_accounts:
  - hdfs
  - spark
  - hadoop
  - spark
  - zookeeper
  - kafka
  - jupyter
  - dbadmin
  - ldapadmin
  - kerberos