---
RANDOM_PASSWD: |
  "{{ '%Y%m%d%H%M%S' | strftime(ansible_date_time.epoch) | password_hash('sha512') | password_hash('sha512') }}"
service_accounts:
  - hdfs
  - spark
  - hadoop
  - spark
  - zookeeper
  - kafka
  - jupyter
  - dbadmin
  - ldapadmin
  - kerberos