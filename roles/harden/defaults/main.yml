---
time_stamp: "{{'%Y%m%d%H%M%S' | strftime(ansible_date_time.epoch)}}"
RANDOM_PASSWD: "{{ time_stamp | password_hash('sha512',time_stamp) }}"
service_accounts:
  - hdfs
  - spark
  - hadoop
  - spark
  - zookeeper
  - kafka
  - jupyter
  - dbadmin
  - ldapadmin
  - kerberos